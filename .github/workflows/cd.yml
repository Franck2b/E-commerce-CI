name: CD - Continuous Deployment

# Déclencheur : GitFlow (main, release/*, hotfix/*)
on:
  push:
    branches:
      - main
      - 'release/**'
      - 'hotfix/**'
    tags:
      - 'v*.*.*'

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    
    environment:
      name: production
      url: http://your-server-ip

    steps:
      # 1. Checkout du code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # 3. Installation des dépendances
      - name: Install dependencies
        run: npm ci

      # 4. Build de l'application
      - name: Build application
        run: npm run build

      # 5. Setup Python pour Ansible
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 6. Installation d'Ansible
      - name: Install Ansible
        run: |
          python -m pip install --upgrade pip
          pip install ansible

      # 7. Configuration SSH
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          chmod 700 ~/.ssh
      
      # 8. Diagnostic réseau
      - name: Network Diagnostic
        run: |
          echo "🔍 Diagnostic réseau vers ${{ secrets.SERVER_HOST }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "📍 Résolution DNS :"
          nslookup ${{ secrets.SERVER_HOST }} || host ${{ secrets.SERVER_HOST }} || echo "Pas de résolution DNS (normal pour IP)"
          echo ""
          echo "📍 Test ping (ICMP) :"
          ping -c 3 -W 2 ${{ secrets.SERVER_HOST }} || echo "⚠️ Ping échoué (peut être normal)"
          echo ""
          echo "📍 Test connectivité port SSH (22) :"
          timeout 15 bash -c "nc -zv ${{ secrets.SERVER_HOST }} 22 2>&1" || echo "⚠️ Port 22 non accessible ou timeout"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
      # 9. Test de connexion SSH avec timeout augmenté
      - name: Test SSH Connection
        run: |
          echo "🔐 Test connexion SSH avec timeout 30s..."
          ssh -v \
              -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              -o ConnectTimeout=30 \
              -o ServerAliveInterval=10 \
              ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
              "echo '✅ SSH Connection OK' && uptime"

      # 10. Exécution du playbook Ansible
      - name: Run Ansible Deployment
        env:
          ANSIBLE_HOST_KEY_CHECKING: 'False'
        run: |
          cd ansible
          ansible-playbook -i inventory/production.yml playbooks/deploy.yml \
            --extra-vars "deploy_version=${{ github.ref_name }}" \
            --extra-vars "ansible_user=${{ secrets.SERVER_USER }}" \
            --extra-vars "ansible_host=${{ secrets.SERVER_HOST }}" \
            -v

      # 11. Nettoyage de la clé SSH
      - name: Cleanup SSH Key
        if: always()
        run: |
          rm -f ~/.ssh/id_rsa
          
      # 12. Notification de succès
      - name: Deployment Success
        if: success()
        run: |
          echo "✅ Déploiement réussi en production!"
          echo "Version: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
          echo "Branche: ${{ github.ref }}"
